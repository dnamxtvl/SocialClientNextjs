"use client"
import { createContext } from 'react'

export const Context = createContext({
  sortListConversations: (data: any) => {
    // Placeholder function
  },
  
  seenMessageOfConversation: (conversationId: string) => {
    // Placeholder function
  }
})
